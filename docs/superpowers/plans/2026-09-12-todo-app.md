# TODO アプリ 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 既存の Lacinia 学習サンドボックスに、ブラウザ (React + Apollo) から GraphQL 経由で MySQL の TODO を読み書きできる最小のフルスタック構成を足す。

**Architecture:** Vite dev サーバ (:5173) が `/api` を Pedestal (:8888) へ proxy し、Lacinia の resolver が `next.jdbc` で Docker 上の MySQL (host :3307) を読み書きする。SQL は `gql.db` に閉じ込め、resolver は変換と委譲のみを行う。

**Tech Stack:** Clojure 1.12 / Lacinia 1.3 / lacinia-pedestal 1.3 / next.jdbc 1.3.1118 / MySQL Connector/J 9.3.0 / MySQL 8.4 (Docker) / React 19 / Vite 8 / Apollo Client 4

**Spec:** `docs/superpowers/specs/2026-09-12-todo-app-design.md`

## Global Constraints

- 既存の `hello` / `greet` クエリは削除しない。学習メモとして残す。
- DB 名 `todo` / ユーザ `todo` / パスワード `todopass` / ホスト側ポート `3307`。この値を全ファイルで揃える。
- マイグレーションツール (migratus 等) は導入しない。スキーマは `db/init/01-schema.sql` のみ。
- コネクションプール (HikariCP) は導入しない。
- サーバ側に CORS 設定は入れない。オリジン差は Vite の proxy で吸収する。
- Apollo のキャッシュ手動更新 (`update` / `cache.modify`) は使わない。`refetchQueries` を使う。
- フロントエンドにテストは置かない。
- コメント・UI 文言は日本語。既存ファイルのコメント密度に合わせる。
- Apollo Client 4 では React フックの import 元が `@apollo/client/react`、`gql` は `@apollo/client`。混同しない。

---

### Task 1: MySQL コンテナと初期スキーマ

**Files:**
- Create: `docker-compose.yml`
- Create: `db/init/01-schema.sql`
- Modify: `.gitignore` (存在しなければ Create)

**Interfaces:**
- Consumes: なし
- Produces: `localhost:3307` で待ち受ける MySQL。DB `todo`、ユーザ `todo`/`todopass`、テーブル `todos (id INT PK AI, title VARCHAR(255) NOT NULL, done BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP)`

- [ ] **Step 1: 初期スキーマ SQL を書く**

`db/init/01-schema.sql`:

```sql
-- コンテナの初回起動時にだけ実行される (docker-entrypoint-initdb.d)。
-- 作り直したいときは `docker compose down -v` でボリュームごと消すこと。
CREATE TABLE IF NOT EXISTS todos (
  id         INT AUTO_INCREMENT PRIMARY KEY,
  title      VARCHAR(255) NOT NULL,
  done       BOOLEAN      NOT NULL DEFAULT FALSE,
  created_at TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

- [ ] **Step 2: docker-compose.yml を書く**

`docker-compose.yml`:

```yaml
# 学習用の MySQL。認証情報はローカル前提でそのまま書いている。
services:
  mysql:
    image: mysql:8.4
    container_name: sandbox-clojure-mysql
    environment:
      MYSQL_ROOT_PASSWORD: rootpass
      MYSQL_DATABASE: todo
      MYSQL_USER: todo
      MYSQL_PASSWORD: todopass
    ports:
      # ローカルに MySQL が入っていても衝突しないよう host 側は 3307 にする
      - "3307:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "127.0.0.1", "-utodo", "-ptodopass"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  mysql-data:
```

- [ ] **Step 3: .gitignore にフロントエンドの生成物を足しておく**

`.gitignore` に以下を追記する (ファイルが無ければ新規作成)。Task 4 で `frontend/` を作るときに必要になる。

```
.cpcache/
target/
frontend/node_modules/
frontend/dist/
```

- [ ] **Step 4: 起動して healthy になるのを確認する**

Run:

```bash
docker compose up -d
docker compose ps
```

Expected: `mysql` の STATUS が `Up ... (healthy)`。`(health: starting)` の場合は 10 秒ほど待って再実行する。

- [ ] **Step 5: テーブルができていることを確認する**

Run:

```bash
docker compose exec mysql mysql -utodo -ptodopass todo -e "DESCRIBE todos;"
```

Expected: `id` / `title` / `done` / `created_at` の 4 行が表示される。
表示されない場合はボリュームが古い。`docker compose down -v && docker compose up -d` でやり直す。

- [ ] **Step 6: ホストから 3307 に繋がることを確認する**

Run:

```bash
docker compose exec mysql mysql -h 127.0.0.1 -P 3306 -utodo -ptodopass todo -e "SELECT COUNT(*) FROM todos;"
```

Expected: `COUNT(*)` が `0`。

- [ ] **Step 7: コミット**

```bash
git add docker-compose.yml db/init/01-schema.sql .gitignore
git commit -m "MySQL コンテナと todos テーブルの初期スキーマを追加"
```

---

### Task 2: DB アクセス層 `gql.db`

**Files:**
- Create: `src/gql/db.clj`
- Create: `test/gql/db_test.clj`
- Modify: `deps.edn` (`:deps` に 2 件追加、`:aliases` に `:test` 追加)

**Interfaces:**
- Consumes: Task 1 の MySQL (`localhost:3307`, DB `todo`)
- Produces: ネームスペース `gql.db` の 4 関数。返す行は next.jdbc 既定のテーブル名付きキーワード (`:todos/id` `:todos/title` `:todos/done` `:todos/created_at`)。
  - `(list-todos)` → 行のベクタ。`created_at` 降順、同値なら `id` 降順
  - `(create-todo! title)` → 挿入した 1 行
  - `(toggle-todo! id)` → 反転後の 1 行。該当 id が無ければ `nil`
  - `(delete-todo! id)` → `true` / `false`

- [ ] **Step 1: deps.edn に依存とテストエイリアスを足す**

`deps.edn` の `:deps` マップに追記する (既存の行はそのまま):

```clojure
         ;; MySQL への接続 (JDBC)
         com.github.seancorfield/next.jdbc {:mvn/version "1.3.1118"}
         com.mysql/mysql-connector-j       {:mvn/version "9.3.0"}
```

`:aliases` マップに追記する:

```clojure
  ;; テスト実行: clj -M:test
  :test {:extra-paths ["test"]
         :extra-deps  {io.github.cognitect-labs/test-runner
                       {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts   ["-m" "cognitect.test-runner"]}
```

依存が解決できることを確認する。Run: `clj -Stree -M:test > /dev/null && echo OK`
Expected: `OK`

- [ ] **Step 2: 失敗するテストを書く**

`test/gql/db_test.clj`:

```clojure
(ns gql.db-test
  "gql.db の結合テスト。Docker の MySQL が起動している前提。
    docker compose up -d
    clj -M:test"
  (:require [clojure.test :refer [deftest is testing]]
            [gql.db :as db]))

(defn- cleanup!
  "テストが作った行を消して DB を元の状態に戻す。"
  [id]
  (when id (db/delete-todo! id)))

(deftest crud-cycle
  (let [created (db/create-todo! "テスト用のタスク")
        id      (:todos/id created)]
    (try
      (testing "作成した行が返ってくる"
        (is (some? id))
        (is (= "テスト用のタスク" (:todos/title created))))

      (testing "一覧に現れる"
        (is (contains? (set (map :todos/id (db/list-todos))) id)))

      (testing "toggle で done が反転する"
        (let [before (:todos/done (first (filter #(= id (:todos/id %)) (db/list-todos))))
              after  (:todos/done (db/toggle-todo! id))]
          (is (not= (boolean before) (boolean after)))))

      (testing "存在しない id の toggle は nil"
        (is (nil? (db/toggle-todo! -1))))

      (testing "削除すると一覧から消える"
        (is (true? (db/delete-todo! id)))
        (is (not (contains? (set (map :todos/id (db/list-todos))) id))))

      (testing "存在しない id の削除は false"
        (is (false? (db/delete-todo! -1))))

      (finally
        (cleanup! id)))))
```

- [ ] **Step 3: テストを走らせて落ちることを確認する**

Run: `clj -M:test -n gql.db-test`
Expected: FAIL。`gql.db` が存在しないので `Could not locate gql/db__init.class` のような読み込みエラーになる。

- [ ] **Step 4: gql.db を実装する**

`src/gql/db.clj`:

```clojure
(ns gql.db
  "MySQL への接続と SQL をここに閉じ込める。
  resolver から SQL が見えないようにするのが目的。

  前提: docker compose up -d で MySQL が起動していること。"
  (:require [next.jdbc :as jdbc]))

(def db-spec
  "接続情報。docker-compose.yml と揃えること。"
  {:dbtype   "mysql"
   :host     "localhost"
   :port     3307
   :dbname   "todo"
   :user     "todo"
   :password "todopass"})

;; DataSource は使い回す。接続プールは学習用途では要らないので入れていない。
(defonce ^:private ds (jdbc/get-datasource db-spec))

(defn list-todos
  "全件を新しい順で返す。"
  []
  (jdbc/execute! ds ["SELECT * FROM todos ORDER BY created_at DESC, id DESC"]))

(defn create-todo!
  "1 件追加して、追加された行を返す。

  LAST_INSERT_ID() は接続ごとの値なので、INSERT と SELECT を
  同じ接続で実行するためにトランザクションで囲っている。"
  [title]
  (jdbc/with-transaction [tx ds]
    (jdbc/execute-one! tx ["INSERT INTO todos (title) VALUES (?)" title])
    (jdbc/execute-one! tx ["SELECT * FROM todos WHERE id = LAST_INSERT_ID()"])))

(defn toggle-todo!
  "done を反転して、反転後の行を返す。該当 id が無ければ nil。"
  [id]
  (jdbc/with-transaction [tx ds]
    (let [updated (:next.jdbc/update-count
                   (jdbc/execute-one! tx ["UPDATE todos SET done = NOT done WHERE id = ?" id]))]
      (when (pos? updated)
        (jdbc/execute-one! tx ["SELECT * FROM todos WHERE id = ?" id])))))

(defn delete-todo!
  "1 件削除する。削除できたら true、該当 id が無ければ false。"
  [id]
  (pos? (:next.jdbc/update-count
         (jdbc/execute-one! ds ["DELETE FROM todos WHERE id = ?" id]))))
```

- [ ] **Step 5: テストが通ることを確認する**

Run:

```bash
docker compose up -d
clj -M:test -n gql.db-test
```

Expected: `Ran 1 tests containing 8 assertions. 0 failures, 0 errors.`

- [ ] **Step 6: コミット**

```bash
git add deps.edn src/gql/db.clj test/gql/db_test.clj
git commit -m "next.jdbc で MySQL の todos を読み書きする gql.db を追加"
```

---

### Task 3: GraphQL スキーマと resolver

**Files:**
- Modify: `resources/schema.edn` (`:objects` と `:mutations` を新設、`:queries` に 1 件追加)
- Modify: `src/gql/schema.clj` (require 追加、resolver 4 つと変換ヘルパ追加、`resolver-map` に 4 件追加)
- Create: `test/gql/schema_test.clj`

**Interfaces:**
- Consumes: `gql.db` の `list-todos` / `create-todo!` / `toggle-todo!` / `delete-todo!`
- Produces: GraphQL の `todos` / `createTodo` / `toggleTodo` / `deleteTodo`。`Todo` 型のフィールドは `id: Int!` `title: String!` `done: Boolean!` `createdAt: String`

- [ ] **Step 1: 失敗するテストを書く**

`test/gql/schema_test.clj`:

```clojure
(ns gql.schema-test
  "コンパイル済みスキーマに対してクエリを実行する結合テスト。
  Docker の MySQL が起動している前提。"
  (:require [clojure.test :refer [deftest is testing]]
            [com.walmartlabs.lacinia :as lacinia]
            [gql.db :as db]
            [gql.schema :as gql-schema]))

(defn- q
  "コンパイル済みスキーマに対してクエリを 1 本実行する。"
  [query variables]
  (lacinia/execute (gql-schema/load-schema) query variables nil))

(deftest todo-operations
  (let [created (-> (q "mutation($t:String!){ createTodo(title:$t){ id title done createdAt } }"
                       {:t "スキーマテスト用のタスク"})
                    (get-in [:data :createTodo]))
        id      (:id created)]
    (try
      (testing "createTodo が作成した Todo を返す"
        (is (some? id))
        (is (= "スキーマテスト用のタスク" (:title created)))
        (is (false? (:done created)))
        (is (string? (:createdAt created))))

      (testing "todos に含まれる"
        (let [ids (->> (q "{ todos { id } }" nil) :data :todos (map :id) set)]
          (is (contains? ids id))))

      (testing "toggleTodo で done が true になる"
        (is (true? (-> (q "mutation($i:Int!){ toggleTodo(id:$i){ done } }" {:i id})
                       (get-in [:data :toggleTodo :done])))))

      (testing "存在しない id の toggleTodo はエラーを返す"
        (let [result (q "mutation($i:Int!){ toggleTodo(id:$i){ done } }" {:i -1})]
          (is (seq (:errors result)))))

      (testing "deleteTodo が true を返す"
        (is (true? (-> (q "mutation($i:Int!){ deleteTodo(id:$i) }" {:i id})
                       (get-in [:data :deleteTodo])))))

      (testing "存在しない id の deleteTodo はエラーを返す"
        (is (seq (:errors (q "mutation($i:Int!){ deleteTodo(id:$i) }" {:i -1})))))

      (finally
        (when id (db/delete-todo! id))))))
```

- [ ] **Step 2: テストを走らせて落ちることを確認する**

Run: `clj -M:test -n gql.schema-test`
Expected: FAIL。`createTodo` がスキーマに無いため `:errors` に "Cannot query field" 系のメッセージが入り、`id` が `nil` になって最初の `is` が落ちる。

- [ ] **Step 3: schema.edn に Todo 型と操作を足す**

`resources/schema.edn` を次の内容にする (既存の `:queries` の 2 件はそのまま残す):

```clojure
;; Lacinia のスキーマ定義 (EDN)
;;
;; GraphQL の SDL (type Query { hello: String }) を EDN で書いたもの。
;; :resolve に書いたキーワードは、実際の関数と gql.schema で紐付ける。
{:objects
 {:Todo
  {:description "TODO 1 件"
   :fields
   {:id        {:type (non-null Int)}
    :title     {:type (non-null String)}
    :done      {:type (non-null Boolean)}
    :createdAt {:type String
                :description "作成日時 (ISO-8601 形式の文字列)"}}}}

 :queries
 {:hello
  {:type        String
   :description "引数なしの動作確認用クエリ。固定文字列を返す。"
   :resolve     :query/hello}

  :greet
  {:type        String
   :description "引数ありの動作確認用クエリ。name を受け取って挨拶を返す。"
   :args        {:name {:type          String
                        :default-value "world"
                        :description   "挨拶する相手の名前"}}
   :resolve     :query/greet}

  :todos
  {:type        (non-null (list (non-null :Todo)))
   :description "TODO を新しい順で全件返す。"
   :resolve     :query/todos}}

 :mutations
 {:createTodo
  {:type        :Todo
   :description "TODO を 1 件追加する。"
   :args        {:title {:type (non-null String)}}
   :resolve     :mutation/create-todo}

  :toggleTodo
  {:type        :Todo
   :description "TODO の done を反転する。"
   :args        {:id {:type (non-null Int)}}
   :resolve     :mutation/toggle-todo}

  :deleteTodo
  {:type        Boolean
   :description "TODO を 1 件削除する。"
   :args        {:id {:type (non-null Int)}}
   :resolve     :mutation/delete-todo}}}
```

- [ ] **Step 4: schema.clj に resolver を足す**

`src/gql/schema.clj` の `:require` を次のようにする:

```clojure
  (:require [clojure.edn :as edn]
            [clojure.java.io :as io]
            [com.walmartlabs.lacinia.resolve :as resolve]
            [com.walmartlabs.lacinia.schema :as schema]
            [com.walmartlabs.lacinia.util :as util]
            [gql.db :as db])
```

既存の `resolve-greet` の下、`resolver-map` の上に追記する:

```clojure
;; ---------- DB の行を GraphQL のフィールドへ変換する ----------

(defn- ->boolean
  "JDBC ドライバによっては BOOLEAN (TINYINT(1)) が数値で返るため、両方を受ける。"
  [v]
  (if (number? v) (pos? v) (boolean v)))

(defn- ->iso-string
  "java.sql.Timestamp / java.time.LocalDateTime のどちらでも ISO-8601 文字列にする。"
  [v]
  (when v
    (if (instance? java.sql.Timestamp v)
      (str (.toInstant ^java.sql.Timestamp v))
      (str v))))

(defn- row->todo
  "next.jdbc が返す :todos/xxx のマップを、GraphQL の Todo 型に合わせて詰め替える。"
  [row]
  {:id        (:todos/id row)
   :title     (:todos/title row)
   :done      (->boolean (:todos/done row))
   :createdAt (->iso-string (:todos/created_at row))})

;; ---------- TODO の resolver ----------

(defn- resolve-todos
  [_context _args _value]
  (mapv row->todo (db/list-todos)))

(defn- resolve-create-todo
  [_context {:keys [title]} _value]
  (row->todo (db/create-todo! title)))

(defn- resolve-toggle-todo
  [_context {:keys [id]} _value]
  (if-let [row (db/toggle-todo! id)]
    (row->todo row)
    ;; resolve-as で nil + GraphQL エラーを返す。例外にはしない。
    (resolve/resolve-as nil {:message "指定された TODO が見つかりません" :id id})))

(defn- resolve-delete-todo
  [_context {:keys [id]} _value]
  (if (db/delete-todo! id)
    true
    (resolve/resolve-as nil {:message "指定された TODO が見つかりません" :id id})))
```

`resolver-map` を次のようにする:

```clojure
(def ^:private resolver-map
  {:query/hello           resolve-hello
   :query/greet           resolve-greet
   :query/todos           resolve-todos
   :mutation/create-todo  resolve-create-todo
   :mutation/toggle-todo  resolve-toggle-todo
   :mutation/delete-todo  resolve-delete-todo})
```

- [ ] **Step 5: テストが通ることを確認する**

Run: `clj -M:test -n gql.schema-test`
Expected: `Ran 1 tests containing 9 assertions. 0 failures, 0 errors.`

- [ ] **Step 6: 全テストを走らせる**

Run: `clj -M:test`
Expected: `0 failures, 0 errors.` (`gql.db-test` と `gql.schema-test` の両方が走る)

- [ ] **Step 7: HTTP 経由でも動くことを確認する**

別ターミナルで `clj -M -m gql.core` を起動してから:

```bash
curl -s -X POST http://localhost:8888/api -H 'content-type: application/json' \
  -d '{"query":"mutation { createTodo(title:\"curl から追加\") { id title done } }"}'
curl -s -X POST http://localhost:8888/api -H 'content-type: application/json' \
  -d '{"query":"{ todos { id title done createdAt } }"}'
```

Expected: 1 件目が `{"data":{"createTodo":{"id":N,"title":"curl から追加","done":false}}}`、2 件目の `todos` にその行が含まれる。確認できたらサーバを止める。

- [ ] **Step 8: コミット**

```bash
git add resources/schema.edn src/gql/schema.clj test/gql/schema_test.clj
git commit -m "Todo 型と todos/createTodo/toggleTodo/deleteTodo を追加"
```

---

### Task 4: フロントエンド (React + Vite + Apollo Client)

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/vite.config.js`
- Create: `frontend/index.html`
- Create: `frontend/src/main.jsx`
- Create: `frontend/src/apollo.js`
- Create: `frontend/src/queries.js`
- Create: `frontend/src/App.jsx`
- Create: `frontend/src/index.css`

**Interfaces:**
- Consumes: Task 3 の GraphQL 操作 (`todos` / `createTodo(title)` / `toggleTodo(id)` / `deleteTodo(id)`)
- Produces: `http://localhost:5173` で動く 1 画面のアプリ

`npm create vite` は対話が入るため使わない。以下のファイルを直接作る。

- [ ] **Step 1: package.json を書く**

`frontend/package.json`:

```json
{
  "name": "todo-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@apollo/client": "^4.3.0",
    "graphql": "^16.14.2",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  }
}
```

`rxjs` は Apollo Client 4 が必須の peer dependency として要求するため入れている。

- [ ] **Step 2: vite.config.js を書く**

`frontend/vite.config.js`:

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    // フロント(:5173) と GraphQL サーバ(:8888) はポートが違うので
    // そのまま fetch すると別オリジン扱いになり CORS で弾かれる。
    // ここで中継することで、ブラウザからは同一オリジンに見える。
    proxy: {
      '/api': 'http://localhost:8888',
    },
  },
})
```

- [ ] **Step 3: index.html とエントリポイントを書く**

`frontend/index.html`:

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TODO</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.jsx"></script>
  </body>
</html>
```

`frontend/src/apollo.js`:

```js
import { ApolloClient, InMemoryCache, HttpLink } from '@apollo/client'

// uri が相対パスなので、Vite の proxy 経由で :8888 の /api に届く。
export const client = new ApolloClient({
  link: new HttpLink({ uri: '/api' }),
  cache: new InMemoryCache(),
})
```

`frontend/src/main.jsx`:

```jsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { ApolloProvider } from '@apollo/client/react'
import { client } from './apollo'
import App from './App'
import './index.css'

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <ApolloProvider client={client}>
      <App />
    </ApolloProvider>
  </StrictMode>,
)
```

- [ ] **Step 4: クエリ定義を書く**

`frontend/src/queries.js`:

```js
import { gql } from '@apollo/client'

export const GET_TODOS = gql`
  query GetTodos {
    todos {
      id
      title
      done
      createdAt
    }
  }
`

export const CREATE_TODO = gql`
  mutation CreateTodo($title: String!) {
    createTodo(title: $title) {
      id
      title
      done
      createdAt
    }
  }
`

export const TOGGLE_TODO = gql`
  mutation ToggleTodo($id: Int!) {
    toggleTodo(id: $id) {
      id
      done
    }
  }
`

export const DELETE_TODO = gql`
  mutation DeleteTodo($id: Int!) {
    deleteTodo(id: $id)
  }
`
```

- [ ] **Step 5: 画面を書く**

`frontend/src/App.jsx`:

```jsx
import { useState } from 'react'
import { useQuery, useMutation } from '@apollo/client/react'
import { GET_TODOS, CREATE_TODO, TOGGLE_TODO, DELETE_TODO } from './queries'

// 更新後の一覧は refetchQueries で取り直す。
// Apollo のキャッシュを手で書き換える方法もあるが、今回は単純さを優先した。
const refetch = { refetchQueries: [GET_TODOS] }

export default function App() {
  const [title, setTitle] = useState('')
  const { data, loading, error } = useQuery(GET_TODOS)
  const [createTodo] = useMutation(CREATE_TODO, refetch)
  const [toggleTodo] = useMutation(TOGGLE_TODO, refetch)
  const [deleteTodo] = useMutation(DELETE_TODO, refetch)

  const onSubmit = async (event) => {
    event.preventDefault()
    const trimmed = title.trim()
    if (!trimmed) return
    await createTodo({ variables: { title: trimmed } })
    setTitle('')
  }

  return (
    <main>
      <h1>TODO</h1>

      <form onSubmit={onSubmit}>
        <input
          value={title}
          onChange={(event) => setTitle(event.target.value)}
          placeholder="やること"
        />
        <button type="submit" disabled={!title.trim()}>
          追加
        </button>
      </form>

      {loading && <p>読み込み中…</p>}
      {error && <p className="error">エラー: {error.message}</p>}

      <ul>
        {data?.todos.map((todo) => (
          <li key={todo.id}>
            <label>
              <input
                type="checkbox"
                checked={todo.done}
                onChange={() => toggleTodo({ variables: { id: todo.id } })}
              />
              <span className={todo.done ? 'done' : undefined}>{todo.title}</span>
            </label>
            <button onClick={() => deleteTodo({ variables: { id: todo.id } })}>
              削除
            </button>
          </li>
        ))}
      </ul>
    </main>
  )
}
```

`frontend/src/index.css`:

```css
body {
  font-family: system-ui, sans-serif;
  margin: 0;
  padding: 2rem 1rem;
  background: #fafafa;
  color: #222;
}

main {
  max-width: 32rem;
  margin: 0 auto;
}

form {
  display: flex;
  gap: 0.5rem;
  margin-bottom: 1.5rem;
}

form input {
  flex: 1;
  padding: 0.5rem;
  font-size: 1rem;
}

ul {
  list-style: none;
  padding: 0;
}

li {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 0.5rem;
  padding: 0.5rem 0;
  border-bottom: 1px solid #e0e0e0;
}

li label {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
}

.done {
  text-decoration: line-through;
  color: #999;
}

.error {
  color: #c00;
}
```

- [ ] **Step 6: 依存をインストールする**

Run: `cd frontend && npm install`
Expected: エラーなく完了する。peer dependency の警告が出た場合は内容を読み、`rxjs` / `graphql` / `react` のバージョン不一致なら `package.json` を合わせて入れ直す。

- [ ] **Step 7: 3 つ起動して動作を確認する**

3 つのターミナルでそれぞれ実行する:

```bash
docker compose up -d          # まだなら
clj -M -m gql.core
cd frontend && npm run dev
```

ブラウザで `http://localhost:5173` を開き、以下を順に確認する:

1. 「やること」に入力して「追加」→ 一覧に追加された行が現れる
2. チェックボックスを押す → 取り消し線が付く。リロードしても状態が残る
3. 「削除」を押す → 行が消える
4. `docker compose exec mysql mysql -utodo -ptodopass todo -e "SELECT * FROM todos;"` で DB の中身が画面と一致する

Expected: 上の 4 つすべて。ブラウザのコンソールに CORS エラーが出る場合は `vite.config.js` の proxy 設定を見直す。

- [ ] **Step 8: コミット**

```bash
git add frontend
git commit -m "React + Vite + Apollo Client の TODO 画面を追加"
```

---

### Task 5: README の更新

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: Task 1〜4 の成果物
- Produces: なし (ドキュメントのみ)

- [ ] **Step 1: README を現状に合わせて書き換える**

`README.md` に対して次の変更を加える:

1. 冒頭の説明文 `DB・認証は無し。ブラウザ (GraphiQL) からクエリを試せる状態がゴール。` を
   `MySQL (Docker) と React フロントエンドを繋いだ TODO アプリまでを一通り動かせる構成。認証は無し。` に変える
2. 「1. インストールするパッケージ」の表に 2 行足す:

   | パッケージ | 内容 | インストール例 (macOS / Homebrew) |
   | --- | --- | --- |
   | Docker Desktop | MySQL をコンテナで動かす | `brew install --cask docker` |
   | Node.js 20.19+ | フロントエンド (Vite) のビルド・dev サーバ | `brew install node` |

3. 「2. 起動」を次の内容に差し替える:

````markdown
## 2. 起動

3 つを順に起動する。

```sh
docker compose up -d                        # MySQL (:3307)
clj -M -m gql.core                          # GraphQL サーバ (:8888)
cd frontend && npm install && npm run dev   # フロントエンド (:5173)
```

| URL | 内容 |
| --- | --- |
| <http://localhost:5173> | TODO アプリ |
| <http://localhost:8888/ide> | GraphiQL |
| <http://localhost:8888/api> | GraphQL エンドポイント (POST) |

DB を初期状態に戻したいときは `docker compose down -v` でボリュームごと削除する。

REPL から起動・停止する場合:

```sh
clj -M:dev          # nREPL (エディタから接続する場合)
```

```clojure
(require '[gql.core :as core])
(core/start!)    ; 起動 (ブロックしない)
(core/restart!)  ; スキーマ・resolver を変更したら作り直す
(core/stop!)     ; 停止
```
````

4. 「3. 動作確認」の末尾に TODO のクエリ例を足す:

````markdown
TODO の操作は GraphiQL から以下で試せる。

```graphql
mutation { createTodo(title: "牛乳を買う") { id title done } }
```

```graphql
{ todos { id title done createdAt } }
```
````

5. 「4. ファイル構成」を次の内容に差し替える:

```
deps.edn              依存とエイリアス (node でいう package.json)
docker-compose.yml    MySQL コンテナ
db/init/01-schema.sql todos テーブル定義 (コンテナ初回起動時に適用)
resources/schema.edn  スキーマ定義 (どんな型・クエリがあるか)
src/gql/schema.clj    resolver と DB の行 → GraphQL の変換
src/gql/db.clj        MySQL への接続と SQL
src/gql/core.clj      HTTP サーバの起動
test/gql/             結合テスト (MySQL 起動が前提。clj -M:test)
frontend/             React + Vite + Apollo Client
```

6. 末尾に「テスト」の節を足す:

````markdown
## 5. テスト

MySQL が起動している状態で実行する。

```sh
docker compose up -d
clj -M:test
```
````

- [ ] **Step 2: 書いた手順どおりに動くか、まっさらな状態から確認する**

Run:

```bash
docker compose down -v && docker compose up -d
clj -M:test
```

Expected: `0 failures, 0 errors.` (README の手順だけで初期化からテストまで通ること)

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "README を MySQL + フロントエンド構成に更新"
```
